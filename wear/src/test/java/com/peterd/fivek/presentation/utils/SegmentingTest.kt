package com.peterd.fivek.presentation.utils

import com.peterd.fivek.presentation.data.week5Run1
import org.junit.Test

// REMEMBER: we want 180 degrees for Total Time - WARMUP - COOLDOWN
class GetSegmentAnglesTest {
    @Test
    fun`SHOULD return angles that add to 180 degrees`() {
        val angles = GetSegmentAngles(week5Run1)
         
    }
}